# Gradle properties
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official

# Sonatype credentials - replace with your actual values or set as environment variables
# ossrhUsername=your_sonatype_username
# ossrhPassword=your_sonatype_password

# GPG signing - replace with your actual values or set as environment variables
# signingKey=your_gpg_private_key
# signingPassword=your_gpg_passphrase
